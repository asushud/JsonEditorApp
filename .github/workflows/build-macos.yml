name: Build JsonEditor Apps

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  # 🪟 Windows Build
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      # 1️⃣ Compile all Java files (multiple mains)
      - name: Compile Java files
        run: |
          mkdir build
          javac -cp ".;jackson-core.jar;jackson-databind.jar;jackson-annotations.jar" -d build *.java

      # 2️⃣ Create fat jar with all dependencies (FIXED)
      - name: Create fat jar
        run: |
          mkdir fat-jar
          # Copy compiled classes
          Copy-Item build\* fat-jar\ -Recurse

          # Extract dependency jars safely (ignore META-INF)
          foreach ($jar in "jackson-core.jar","jackson-databind.jar","jackson-annotations.jar") {
              Write-Host "Extracting $jar"
              $tempDir = "$env:TEMP\jartemp"
              Remove-Item -Recurse -Force $tempDir -ErrorAction SilentlyContinue
              mkdir $tempDir
              [System.IO.Compression.ZipFile]::ExtractToDirectory($jar, $tempDir)
              # Copy only files (not folders) and skip META-INF
              Get-ChildItem $tempDir -Recurse | Where-Object { $_.PSIsContainer -eq $false -and $_.FullName -notmatch "META-INF" } | Copy-Item -Destination fat-jar -Force
              Remove-Item -Recurse -Force $tempDir
          }

          # Create final fat jar with main class
          cd fat-jar
          jar cfe ../JsonEditor-fat.jar JsonEditor *
          cd ..

      # 3️⃣ Create custom runtime image
      - name: Create runtime image
        run: |
          jlink `
            --module-path "$env:JAVA_HOME\jmods" `
            --add-modules java.base,java.desktop,java.logging,java.xml `
            --output jre

      # 4️⃣ Prepare input folder for jpackage
      - name: Prepare input folder
        run: |
          Remove-Item -Recurse -Force build-input -ErrorAction SilentlyContinue
          mkdir build-input
          Copy-Item JsonEditor-fat.jar build-input\

      # 5️⃣ Clean output folder
      - name: Clean output folder
        run: Remove-Item -Recurse -Force output -ErrorAction SilentlyContinue

      # 6️⃣ Package Windows EXE
      - name: Package Windows EXE
        run: |
          jpackage `
            --name JsonEditor `
            --input build-input `
            --main-jar JsonEditor-fat.jar `
            --main-class JsonEditor `
            --runtime-image jre `
            --type app-image `
            --win-console `
            --app-version 1.0 `
            --dest output

      # 7️⃣ Upload Windows artifact
      - name: Upload Windows app-image
        uses: actions/upload-artifact@v4
        with:
          name: JsonEditor-Windows
          path: output/JsonEditor

  # 🍎 macOS Build
  build-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      # 1️⃣ Compile Java files
      - name: Compile Java files
        run: javac -cp ".:jackson-core.jar:jackson-databind.jar:jackson-annotations.jar" -d build *.java

      # 2️⃣ Create fat jar
      - name: Create fat jar
        run: |
          mkdir fat-jar
          cp -R build/* fat-jar/
          jar xf jackson-core.jar
          jar xf jackson-databind.jar
          jar xf jackson-annotations.jar
          cd fat-jar
          jar cfe JsonEditor-fat.jar JsonEditor *
          mv JsonEditor-fat.jar ../

      # 3️⃣ Clean output folder
      - name: Clean output folder
        run: rm -rf output

      # 4️⃣ Package macOS DMG
      - name: Package macOS App
        run: |
          mkdir -p output
          jpackage \
            --name JsonEditor \
            --input . \
            --main-jar JsonEditor-fat.jar \
            --main-class JsonEditor \
            --type dmg \
            --app-version 1.0 \
            --dest output

      # 5️⃣ Upload macOS artifact
      - name: Upload macOS App artifact
        uses: actions/upload-artifact@v4
        with:
          name: JsonEditor-macOS
          path: output/
